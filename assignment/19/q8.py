# Write a python program to multiply all the numbers in a list.
import math


def list_args(students):
        print(math.prod(students))
        # res=1
        # for i in students:
        #     res=res*i
        # print(res)
name=[6,7,8,9]
list_args(name)
